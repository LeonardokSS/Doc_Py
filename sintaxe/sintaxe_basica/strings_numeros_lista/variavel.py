#Para atribuir valores a uma variável basta usar: nome_variável = valor_da_variável, Se uma variável não é “definida” (não tem um valor atribuído).

largura = 20

altura = 10

base = (largura**2) + (altura**2)

input(base)